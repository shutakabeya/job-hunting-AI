import { BaseLoader } from './BaseLoader';
declare class WebPageLoader extends BaseLoader {
    loadData(url: string): Promise<{
        content: string;
        metaData: {
            url: string;
        };
    }[]>;
}
export { WebPageLoader };
