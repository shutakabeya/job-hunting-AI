import type { LoaderResult } from '../models';
import { BaseLoader } from './BaseLoader';
interface Page {
    page_content: string;
}
declare class PdfFileLoader extends BaseLoader {
    static getPagesFromPdf(url: string): Promise<Page[]>;
    loadData(url: string): Promise<LoaderResult>;
}
export { PdfFileLoader };
