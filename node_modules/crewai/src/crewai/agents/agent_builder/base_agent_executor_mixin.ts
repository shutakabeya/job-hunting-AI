const BaseAgentExecutorMixin = {
  async execute(task, context = {}) {
    // Implement execution logic here
    const result = await this._run({ task, context });
    return this.outputConverter.convert(result);
  }
};

export { BaseAgentExecutorMixin };