class BaseAgentTool {
    constructor(name, description, func) {
        this.name = name;
        this.description = description;
        this.func = func;
    }

    async run(input) {
        return await this.func(input);
    }
}

export { BaseAgentTool };