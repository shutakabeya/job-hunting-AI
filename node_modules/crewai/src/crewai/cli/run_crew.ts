import chalk from 'chalk';
import { spawnProcess } from './utils';

async function runCrew() {
  try {
    await spawnProcess('poetry', ['run', 'run_crew']);
  } catch (error) {
    console.error(chalk.red(`Failed to run crew: ${error.message}`));
  }
}

export { runCrew }